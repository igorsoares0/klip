import { RequestResetScreen } from "@/components/screens/reset-password";

export const metadata = { title: "Reset your password · Klip" };

export default function ResetPasswordPage() {
  return <RequestResetScreen />;
}
